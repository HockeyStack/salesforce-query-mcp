server {
    listen 443 ssl;
    server_name revops-mcp.hockeystack.com;

    ssl_certificate     /etc/ssl/revops-mcp/certificate.crt;
    ssl_certificate_key /etc/ssl/revops-mcp/private.key;

    # Slack Events API
    location /slack/events {
        proxy_pass http://localhost:3000/slack/events;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # Slack bot health check
    location /health/bot {
        proxy_pass http://localhost:3000/health;
        proxy_http_version 1.1;
    }

    # MCP server
    location /mcp {
        proxy_pass http://localhost:3001/mcp;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # MCP server health check
    location /health/mcp {
        proxy_pass http://localhost:3001/health;
        proxy_http_version 1.1;
    }
}

server {
    listen 80;
    server_name revops-mcp.hockeystack.com;
    return 301 https://$host$request_uri;
}
